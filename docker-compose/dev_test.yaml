services:
  backend:
    environment:
      TEST: 1
      ALLOWED_HOSTS: '*'

  frontend:
    environment:
      VITE_CYPRESS: 'true'

  postgres:
    volumes:
      - postgresqldata_test:/var/lib/postgresql/data

volumes:
  postgresqldata_test:
    external: true
